---
phase: 05-grade-system-picker
plan: 05
type: execute
wave: 2
depends_on: [05-02, 05-03, 05-04]
files_modified: []
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - D-07
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "Grade system picker row is visible in production at climbing-search.pages.dev"
    - "Switching to V system shows VB-V17 in grade filter selects"
    - "Sort by Grade with V system active sorts boulder routes to the top"
    - "Sort By dropdown has exactly 4 options (no specialty options)"
    - "Worker test suite passes in CI (all grade_system tests green)"
  artifacts:
    - path: "https://climbing-search.pages.dev"
      provides: "Live deployment with picker row visible"
  key_links:
    - from: "Cloudflare Pages (master branch)"
      to: "climbing-search.pages.dev"
      via: "npm run build → wrangler pages deploy"
      pattern: "deploy"
---

<objective>
Deploy the completed Phase 05 implementation: build the frontend, deploy the worker API, deploy Cloudflare Pages, and verify the grade system picker is working in production via a human verification checkpoint.

Purpose: Ships Phase 05 to production and confirms the end-to-end user experience is correct.

Output: Live production deployment at climbing-search.pages.dev with the grade system picker active.
</objective>

<execution_context>
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhirono/Dev/mtnpj/.planning/ROADMAP.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/05-grade-system-picker/05-CONTEXT.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/05-grade-system-picker/05-01-SUMMARY.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/05-grade-system-picker/05-02-SUMMARY.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/05-grade-system-picker/05-03-SUMMARY.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/05-grade-system-picker/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build, then deploy worker and frontend</name>
  <files></files>
  <read_first>
    - worker-api/package.json — confirm deploy script name
    - climbing-search/package.json — confirm build script name
  </read_first>
  <action>
Run the following in sequence. Stop and report any failure immediately.

1. Run full worker test suite:
```bash
cd /Users/jhirono/Dev/mtnpj/worker-api && npm test
```
All tests must pass (green). If any fail, stop — do not deploy.

2. Frontend type check:
```bash
cd /Users/jhirono/Dev/mtnpj/climbing-search && npx tsc --noEmit
```
Must exit 0. If TypeScript errors exist, stop — do not deploy.

3. Build frontend:
```bash
cd /Users/jhirono/Dev/mtnpj/climbing-search && npm run build
```
Must complete without errors.

4. Deploy worker API (per MEMORY.md: always deploy to --branch master):
```bash
cd /Users/jhirono/Dev/mtnpj/worker-api && npx wrangler deploy
```

5. Deploy Cloudflare Pages (per MEMORY.md: master=Preview/test, main=Production):
```bash
cd /Users/jhirono/Dev/mtnpj/climbing-search && npx wrangler pages deploy dist --project-name climbing-search --branch master
```

Report the deployment URLs from the wrangler output.
  </action>
  <verify>
    <automated>cd /Users/jhirono/Dev/mtnpj/worker-api && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Worker tests exit 0
    - TypeScript compiles clean
    - Frontend build completes
    - Worker deployed (wrangler reports success)
    - Pages deployed (wrangler reports preview URL)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 05 Grade System Picker deployed to the master/preview branch of climbing-search.pages.dev.

    Changes deployed:
    - Grade system picker row with 5 segmented buttons (YDS | V | Aid | Ice | Mixed) above the Grade Filter in FilterPanel
    - Active button has solid blue fill (bg-blue-600); inactive buttons are gray
    - Switching systems resets the grade filter and changes the grade select options
    - Sort By dropdown trimmed to 4 options: Grade, Stars, # of Votes, Left to Right
    - "Grade" sort is now polymorphic — switches to V-scale sort when V system is active
    - Non-YDS grade range filter uses LIKE prefix matching (server-side, via grade_list param)
  </what-built>
  <how-to-verify>
    Visit the deployed preview URL (from Task 1 wrangler output) or https://climbing-search.pages.dev

    Check 1 — Picker visible:
    - Open the filter panel. You should see a "Grade System" label with 5 buttons: YDS | V | Aid | Ice | Mixed
    - YDS button should be active (solid blue) on load
    - The picker row is above the Grade Filter section (not inside an accordion)

    Check 2 — System switch:
    - Click "V". The V button turns blue; Grade System row shows V as active
    - Grade Filter checkbox unchecks automatically
    - Enable Grade Filter — the min/max selects should show VB, V0, V1 ... V17 (not YDS grades)
    - Set min=V3, max=V8. Routes should filter to boulder routes in V3-V8 range

    Check 3 — Sort By trimmed:
    - Open Sort By dropdown. Should show exactly: Grade, Stars, # of Votes, Left to Right
    - Aid Grade, Ice Grade, Mixed Grade, Boulder Grade should NOT appear

    Check 4 — Polymorphic grade sort:
    - With V system active, set Sort By to "Grade"
    - Boulder routes (V3, V4, V5, etc.) should sort in V-scale order
    - Routes without a V grade should sort to the bottom

    Check 5 — YDS still works:
    - Click "YDS" to return to YDS mode
    - Grade filter shows 5.3–5.16d options
    - Grade sort and numeric grade filter work as before

    Check 6 — Dark mode (if system is in dark mode):
    - Active button: solid blue fill
    - Inactive buttons: gray-700 background
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Otherwise describe the issue (which check failed, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare Pages deployment | Build artifacts deployed to CDN edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Production deployment | accept | Deployment is via authenticated wrangler CLI; no additional mitigations needed for this phase |
</threat_model>

<verification>
After Plan 05 completes:
- Worker test suite: all tests green
- TypeScript: `npx tsc --noEmit` exits 0
- Pages deployment: wrangler reports success with preview URL
- Human checkpoint: "approved" signal received
</verification>

<success_criteria>
- All worker tests green
- Frontend TypeScript compiles clean
- Cloudflare Pages and Worker deployed successfully
- Human verifies: picker row visible, system switch resets grade filter, grade selects update to active system's list, Sort By has 4 options, grade sort is polymorphic
</success_criteria>

<output>
After completion, create `.planning/phases/05-grade-system-picker/05-05-SUMMARY.md`
</output>
