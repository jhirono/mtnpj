---
phase: 03-tagging-upgrade
plan: "05"
type: execute
wave: 4
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [TAG-05]
user_setup: []
must_haves:
  truths:
    - "Yosemite areas and routes are importable into remote D1 and visible in production"
    - "Yosemite route tags are present in remote D1 (route_tags column non-null for Yosemite routes)"
    - "Washington routes and areas are deleted from remote D1 (0 rows with path LIKE '/washington/%')"
    - "Production D1 contains only Yosemite data (no Nevada, no Washington)"
    - "User has verified the UI shows Yosemite routes"
  artifacts:
    - path: "worker-api/import_yosemite.sql (generated by import_to_d1.py)"
      provides: "INSERT statements for Yosemite areas + routes"
    - path: "worker-api/tag_update_yosemite-national-park_routes_tagged.sql (from Plan 04)"
      provides: "UPDATE statements for Yosemite route_tags and area_tags"
  key_links:
    - from: "data/yosemite-national-park_routes_tagged.json"
      to: "remote Cloudflare D1"
      via: "wrangler d1 execute --remote --file"
      pattern: "wrangler.*--remote"
    - from: "remote D1 areas (path='/washington/...')"
      to: "DELETE operation"
      via: "wrangler d1 execute --remote --command"
      pattern: "DELETE FROM routes.*washington"
---

<objective>
Push Yosemite data to Cloudflare remote D1 (production) and delete Washington data. Order: (1) import Yosemite areas + routes to remote D1, (2) push Yosemite route/area tags to remote D1, (3) delete Washington routes then Washington areas from remote D1 (FK order required). User verifies UI after push. Per D-12, D-13, D-14.

Purpose: TAG-05 (all routes in D1 synced) + the deployment half of the phase. Remote D1 currently has only Washington data (10,951 routes, 2,033 areas). After this plan, remote D1 has Yosemite only (≈2,966 routes, ≈517 areas), making the production dataset geographically coherent.

CRITICAL: Nevada was NEVER pushed to remote D1. Do NOT plan or execute any Nevada deletion. The delete step targets ONLY Washington (path LIKE '/washington/%').

Output: Production Cloudflare D1 updated — Yosemite routes with tags, Washington data removed.
</objective>

<execution_context>
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhirono/Dev/mtnpj/.planning/ROADMAP.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-CONTEXT.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-RESEARCH.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-04-SUMMARY.md
</context>

<interfaces>
<!-- Remote D1 state going in (VERIFIED by wrangler query): -->
<!-- 10,951 routes — all Washington (path LIKE '/washington/%') -->
<!-- 2,033 areas — all Washington (path LIKE '/washington/%') -->
<!-- 0 Yosemite routes, 0 Nevada routes -->
<!-- No tick comments in remote D1 -->

<!-- wrangler CLI pattern for remote D1 operations: -->
<!--   npx wrangler d1 execute climbing-search --remote --file <sql_file> -->
<!--   npx wrangler d1 execute climbing-search --remote --command "<SQL>" -->

<!-- import_to_d1.py generates chunked INSERT SQL for areas + routes -->
<!--   python3 scraping/import_to_d1.py data/yosemite-national-park_routes_tagged.json -->
<!--   Output: worker-api/import_yosemite.sql (filename may vary — check script's argparse) -->

<!-- FK constraint: routes.area_id REFERENCES areas(area_id) -->
<!-- → areas must be inserted BEFORE routes (import_to_d1.py handles this) -->
<!-- → routes must be DELETED BEFORE areas (deletion step) -->

<!-- Delete SQL (VERIFIED against remote D1 path format '/washington/...'): -->
<!-- DELETE FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '/washington/%'); -->
<!-- DELETE FROM areas WHERE path LIKE '/washington/%'; -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate import SQL and push Yosemite data to remote D1</name>
  <files></files>
  <read_first>
    - scraping/import_to_d1.py (grep for argparse / add_argument to find exact CLI syntax and output file name pattern)
    - worker-api/tag_update_yosemite-national-park_routes_tagged.sql (confirm this exists from Plan 04)
    - data/yosemite-national-park_routes_tagged.json (confirm this exists: ls -lh)
  </read_first>
  <action>
    Step 1 — Verify prerequisites from Plan 04:
      ls -lh data/yosemite-national-park_routes_tagged.json     # Must exist
      ls -lh worker-api/tag_update_yosemite-national-park_routes_tagged.sql  # Must exist

    Step 2 — Verify wrangler auth and current remote D1 state:
      npx wrangler d1 execute climbing-search --remote \
        --command "SELECT COUNT(*) as wa_routes FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '/washington/%');"
      Expected: 10951 (Washington routes still present before delete)

      npx wrangler d1 execute climbing-search --remote \
        --command "SELECT COUNT(*) as yos_routes FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '%yosemite%');"
      Expected: 0 (Yosemite not yet in remote)

    Step 3 — Generate Yosemite import SQL:
      Read scraping/import_to_d1.py to find the CLI flags. Common pattern:
        python3 scraping/import_to_d1.py data/yosemite-national-park_routes_tagged.json
      Or with explicit output:
        python3 scraping/import_to_d1.py data/yosemite-national-park_routes_tagged.json \
          -o worker-api/import_yosemite.sql
      Confirm the output file: ls -lh worker-api/import_yosemite.sql (or whatever the script names it)
      Estimated size: ~3.5MB, ~90 INSERT statements chunked at 100KB each

    Step 4 — Push Yosemite areas + routes to remote D1:
      npx wrangler d1 execute climbing-search --remote \
        --file worker-api/import_yosemite.sql
      This may take several minutes for a 3.5MB file.
      If wrangler splits execution across multiple API calls, it will print progress per statement.

    Step 5 — Verify Yosemite routes are now in remote D1:
      npx wrangler d1 execute climbing-search --remote \
        --command "SELECT COUNT(*) FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '%yosemite%');" \
        --json
      Expected: ~2966

    Step 6 — Push Yosemite tags to remote D1:
      npx wrangler d1 execute climbing-search --remote \
        --file worker-api/tag_update_yosemite-national-park_routes_tagged.sql
      This runs the UPDATE statements generated by d1_tag_sync.py.

    Step 7 — Verify Yosemite tags are in remote D1:
      npx wrangler d1 execute climbing-search --remote \
        --command "SELECT COUNT(*) FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '%yosemite%') AND route_tags IS NOT NULL;" \
        --json
      Expected: > 2000

    Step 8 — Delete Washington routes FIRST (FK order — routes before areas):
      npx wrangler d1 execute climbing-search --remote \
        --command "DELETE FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '/washington/%');"

    Step 9 — Delete Washington areas (after routes deleted):
      npx wrangler d1 execute climbing-search --remote \
        --command "DELETE FROM areas WHERE path LIKE '/washington/%';"

    Step 10 — Verify Washington is gone from remote D1:
      npx wrangler d1 execute climbing-search --remote \
        --command "SELECT COUNT(*) as wa_check FROM areas WHERE path LIKE '/washington/%';" \
        --json
      Expected: 0

      npx wrangler d1 execute climbing-search --remote \
        --command "SELECT COUNT(*) as total_routes FROM routes;" \
        --json
      Expected: ~2966 (Yosemite only)
  </action>
  <verify>
    <automated>cd /Users/jhirono/Dev/mtnpj && npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) as wa_check FROM areas WHERE path LIKE '/washington/%';" --json 2>/dev/null | python3 -c "import sys,json; r=json.load(sys.stdin); print('WA areas remaining:', r[0]['results'][0]['wa_check'])"</automated>
  </verify>
  <acceptance_criteria>
    - `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM areas WHERE path LIKE '/washington/%';" --json` → 0
    - `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '%yosemite%');" --json` → value between 2900 and 3000
    - `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '%yosemite%') AND route_tags IS NOT NULL;" --json` → value > 2000
    - `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM routes;" --json` → same value as Yosemite count (no Washington routes remaining)
  </acceptance_criteria>
  <done>Remote D1 has 0 Washington areas; Yosemite route count between 2900–3000 in remote D1; >2000 routes with non-null route_tags in remote D1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies production UI shows Yosemite routes</name>
  <read_first>
    - climbing-search/src/main.tsx (to find the production URL if not known)
  </read_first>
  <what-built>
    Remote Cloudflare D1 now contains:
    - ~2,966 Yosemite NP routes with tags
    - ~517 Yosemite NP areas
    - 0 Washington routes (deleted)
    Production worker API serves Yosemite data only.
  </what-built>
  <how-to-verify>
    Step 1 — Open the production URL in a browser. The Cloudflare Pages URL is typically:
      https://climbing-search.pages.dev  (or your custom domain if configured)

    Step 2 — Search for a known Yosemite route (e.g., "The Nose", "Slab Daddy", "Swan Slab"):
      - Routes should appear in search results
      - Tags should be visible on route cards

    Step 3 — Verify Washington routes are gone:
      - Search for a known Washington route (e.g., "Freeway", "Lunatic Fringe")
      - Expected: no results (or "no routes found")

    Step 4 — Spot-check a route with tick data:
      - Find a well-known Yosemite route and verify tags seem reasonable

    Step 5 — Per D-14: UI verification is manual only; no automated browser test in this phase.

    If UI looks correct → type "approved" to mark Phase 03 complete.
    If issues found → describe the problem (wrong data, missing routes, broken UI).
  </how-to-verify>
  <resume-signal>
    Type "approved" to confirm Phase 03 is complete and production is live with Yosemite data.
    Or describe issues found for investigation.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → Cloudflare D1 remote | wrangler CLI executes SQL against production DB using wrangler auth token |
| DELETE operation | Irreversible deletion of 10,951 Washington routes and 2,033 areas from production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | DELETE FROM routes/areas targets wrong region | mitigate | Path filter '/washington/%' is verified against actual remote D1 path format. Routes must be deleted BEFORE areas (FK). Acceptance criteria confirms 0 WA areas remain after delete. |
| T-03-16 | Tampering | Nevada deletion accidentally triggered | mitigate | Nevada was NEVER in remote D1 (verified by wrangler query: 0 NV routes). Only 2 DELETE commands are run, both with '/washington/%' path filter. No Nevada-related SQL anywhere in this plan. |
| T-03-17 | Denial of Service | Large import SQL file (3.5MB) causing wrangler timeout | mitigate | import_to_d1.py uses 100KB-chunked INSERT statements (established pattern from Phase 01 Nevada import which was 8.3MB and succeeded). Wrangler processes each statement independently. |
| T-03-18 | Information Disclosure | wrangler auth token stored on machine | accept | Auth token managed by wrangler's own credential store. Not committed to git. Single-developer machine. |
| T-03-19 | Elevation of Privilege | Wrangler token has write access to production D1 | accept | This is the intended access level for this deployment step. Wrangler tokens are scoped to specific account/DB via wrangler.toml. No mitigation beyond correct auth scope. |
</threat_model>

<verification>
1. `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM areas WHERE path LIKE '/washington/%';" --json` → 0
2. `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM routes WHERE area_id IN (SELECT area_id FROM areas WHERE path LIKE '%yosemite%');" --json` → ~2966
3. `npx wrangler d1 execute climbing-search --remote --command "SELECT COUNT(*) FROM routes WHERE route_tags IS NOT NULL;" --json` → > 2000
4. Production UI: Yosemite routes appear in search, Washington routes do not
5. User checkpoint approved
</verification>

<success_criteria>
- Remote D1 contains ~2,966 Yosemite routes and ~517 Yosemite areas
- Remote D1 contains 0 Washington routes and 0 Washington areas
- Yosemite routes in remote D1 have route_tags populated (> 2000 non-null)
- Production UI shows Yosemite routes with tags
- TAG-01 through TAG-05 all complete — Phase 03 done
</success_criteria>

<output>
After completion, create /Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-05-SUMMARY.md
Record: final remote D1 counts (routes, areas, tagged routes), Washington deletion confirmed, UI verification result.
Update .planning/STATE.md: phase 03 complete, milestone v1.1 shipped.
Update .planning/REQUIREMENTS.md: TAG-01 through TAG-05 marked complete with date.
</output>
