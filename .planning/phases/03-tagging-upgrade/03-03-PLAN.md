---
phase: 03-tagging-upgrade
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - tagging/route_area_tagging.py
autonomous: true
requirements: [TAG-02, TAG-03]
must_haves:
  truths:
    - "route_area_tagging.py has a module-level TAGGING_MODEL constant set to the winning model ID"
    - "create_batch_requests() at line 192 uses TAGGING_MODEL instead of hardcoded 'gpt-4o-mini'"
    - "create_area_batch_requests() at line 449 uses TAGGING_MODEL instead of hardcoded 'gpt-4o-mini'"
    - "No other hardcoded 'gpt-4o-mini' strings remain in tagging logic (grep confirms)"
    - "The route_tick_comments slot at line 185 is confirmed functional with D1-enriched data"
  artifacts:
    - path: "tagging/route_area_tagging.py"
      provides: "Tagging pipeline with winning model and enriched tick context"
      contains: "TAGGING_MODEL"
  key_links:
    - from: "tagging/route_area_tagging.py TAGGING_MODEL"
      to: "create_batch_requests() line 192"
      via: "constant reference"
      pattern: "TAGGING_MODEL"
    - from: "tagging/route_area_tagging.py TAGGING_MODEL"
      to: "create_area_batch_requests() line 449"
      via: "constant reference"
      pattern: "TAGGING_MODEL"
---

<objective>
Migrate route_area_tagging.py to the winning model selected in Plan 02. Replace BOTH hardcoded "gpt-4o-mini" strings (line 192 in create_batch_requests() and line 449 in create_area_batch_requests()) with a single TAGGING_MODEL module-level constant. Confirm the route_tick_comments slot at line 185 will receive enriched D1 data when called with the enriched JSON from Plan 01.

Purpose: TAG-02 (pipeline migrated to winning model) and TAG-03 (prompts leverage tick comment data). The tick comment slot already exists at line 185 — the enrichment from Plan 01 feeds it. The model migration ensures the full 2,969-route run uses the winner, not the gpt-4o-mini baseline. Per D-05, D-06, D-07.

Output: tagging/route_area_tagging.py (two line changes + one constant added)
</objective>

<execution_context>
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhirono/Dev/mtnpj/.planning/ROADMAP.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-CONTEXT.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-RESEARCH.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-02-SUMMARY.md
@/Users/jhirono/Dev/mtnpj/tagging/route_area_tagging.py
</context>

<interfaces>
<!-- Exact lines that must change. Read tagging/route_area_tagging.py before editing. -->

Constants section (after imports, around line 18-23):
  MAX_BATCH_SIZE = 50000
  MAX_RETRIES = 3
  RETRY_DELAY = 5
  LOG_FILE = "tagging_validation.log"
  → ADD: TAGGING_MODEL = "<winning_model_from_03-02-SUMMARY>"

create_batch_requests() body, line 192:
  BEFORE: "model": "gpt-4o-mini",
  AFTER:  "model": TAGGING_MODEL,

create_area_batch_requests() body, line 449:
  BEFORE: "model": "gpt-4o-mini",
  AFTER:  "model": TAGGING_MODEL,

route_tick_comments slot (line 185 — READ ONLY, do NOT modify):
  Comments: {route.get('route_tick_comments', '')} {' '.join([c.get('comment_text', '') for c in route.get('route_comments', [])])}
  (This slot already exists and will receive D1-enriched data when called with the enriched JSON)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add TAGGING_MODEL constant and replace both hardcoded model strings</name>
  <files>tagging/route_area_tagging.py</files>
  <read_first>
    - tagging/route_area_tagging.py (full file — confirm exact current content of lines 18-23, 190-195, 448-453 before editing)
    - .planning/phases/03-tagging-upgrade/03-02-SUMMARY.md (extract the winning model ID — it will be "gpt-4o-mini", "gpt-5-nano", or "gpt-5-mini")
  </read_first>
  <action>
    Step 1 — Read the winning model ID from .planning/phases/03-tagging-upgrade/03-02-SUMMARY.md.
    The SUMMARY will record which model the user selected. Extract the exact model ID string
    (e.g., "gpt-5-nano" or "gpt-4o-mini" or "gpt-5-mini").

    Step 2 — Read tagging/route_area_tagging.py. Confirm:
      - Line 192 contains exactly: `"model": "gpt-4o-mini",`
      - Line 449 contains exactly: `"model": "gpt-4o-mini",`
      - The constants block (lines ~18-23) contains MAX_BATCH_SIZE, MAX_RETRIES, RETRY_DELAY, LOG_FILE.

    Step 3 — Add TAGGING_MODEL constant. Insert after the existing constants block (after LOG_FILE line):
      TAGGING_MODEL = "<winning_model_id>"   # Set by Phase 03 benchmark (03-02). Options: gpt-4o-mini, gpt-5-nano, gpt-5-mini

    Step 4 — Replace line 192:
      Change: `"model": "gpt-4o-mini",`
      To:     `"model": TAGGING_MODEL,`
      This is inside create_batch_requests() function.

    Step 5 — Replace line 449:
      Change: `"model": "gpt-4o-mini",`
      To:     `"model": TAGGING_MODEL,`
      This is inside create_area_batch_requests() function.
      NOTE: This is the line most often missed. Verify it is changed.

    Step 6 — Verify no other "gpt-4o-mini" strings remain in tagging logic:
      grep -n '"gpt-4o-mini"' tagging/route_area_tagging.py
      Expected: 0 results. If any remain, they are in comments or docstrings — that is acceptable.
      Any remaining occurrences in ACTIVE CODE (outside comments/strings) must be changed.

    Step 7 — Verify line 185 route_tick_comments slot is intact (do NOT modify it):
      grep -n "route_tick_comments" tagging/route_area_tagging.py
      Must return at least 1 line containing "route.get('route_tick_comments', '')"

    Step 8 — Syntax check:
      python3 -c "import ast; ast.parse(open('tagging/route_area_tagging.py').read()); print('Syntax OK')"
  </action>
  <verify>
    <automated>cd /Users/jhirono/Dev/mtnpj && python3 -c "import ast; ast.parse(open('tagging/route_area_tagging.py').read()); print('Syntax OK')" && grep -n "TAGGING_MODEL" tagging/route_area_tagging.py && grep -v "^#\|#.*gpt-4o-mini\|'gpt-4o-mini'" tagging/route_area_tagging.py | grep -c '"gpt-4o-mini"' | grep "^0$" && grep -n "route_tick_comments" tagging/route_area_tagging.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TAGGING_MODEL" tagging/route_area_tagging.py` returns 3 lines:
        1. The constant definition line: `TAGGING_MODEL = "gpt-5-nano"` (or whichever model won)
        2. Line ~192: `"model": TAGGING_MODEL,` (inside create_batch_requests)
        3. Line ~449: `"model": TAGGING_MODEL,` (inside create_area_batch_requests)
    - `grep -n '"gpt-4o-mini"' tagging/route_area_tagging.py | grep -v "^[0-9]*:#"` returns 0 lines
      (no active-code hardcodes remain; comment lines are acceptable)
    - `grep -n "route_tick_comments" tagging/route_area_tagging.py` returns at least 1 line
      containing `route.get('route_tick_comments', '')` (slot preserved)
    - `python3 -c "import ast; ast.parse(open('tagging/route_area_tagging.py').read()); print('OK')"` prints "OK"
    - `python3 -m pytest tagging/tests/ -q` — all pre-existing tests still pass
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code → production tagging run | The TAGGING_MODEL constant controls which model is called for every route and area in the full 2,969-route run; wrong model = wrong charges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | Missed second model hardcode at line 449 | mitigate | Acceptance criteria explicitly requires TAGGING_MODEL appears 3 times (definition + 2 usages). grep verification step is part of task action. |
| T-03-09 | Tampering | Unintended modification to route_tick_comments slot (line 185) | mitigate | Acceptance criteria requires grep confirms line 185 slot is intact. Syntax check catches structural breakage. |
| T-03-10 | Repudiation | Which model was used for the full run | accept | TAGGING_MODEL constant in source code + git commit provides an immutable audit trail. |
</threat_model>

<verification>
1. `grep -n "TAGGING_MODEL" tagging/route_area_tagging.py` → 3 lines (definition + 2 usages)
2. `grep -n '"gpt-4o-mini"' tagging/route_area_tagging.py | grep -v "#"` → 0 lines
3. `grep -n "route_tick_comments" tagging/route_area_tagging.py` → at least 1 line with slot
4. `python3 -m pytest tagging/tests/ -q` → all tests pass (was 39 before + 6 from Plan 01)
5. `python3 -c "import ast; ast.parse(open('tagging/route_area_tagging.py').read())"` → exits 0
</verification>

<success_criteria>
- TAGGING_MODEL constant added to route_area_tagging.py constants section
- Both line 192 (route tagging) and line 449 (area tagging) now reference TAGGING_MODEL
- Zero hardcoded "gpt-4o-mini" strings remain in active code
- route_tick_comments slot at line 185 is intact and unchanged
- All tests pass — no regressions
</success_criteria>

<output>
After completion, create /Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-03-SUMMARY.md
</output>
