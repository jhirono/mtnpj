---
phase: 03-tagging-upgrade
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tagging/enrich_ticks.py
  - tagging/tests/test_enrich_ticks.py
autonomous: true
requirements: [TAG-03]
must_haves:
  truths:
    - "data/yosemite-national-park_routes_enriched.json exists and all 2,966 routes are present"
    - "174 routes that have tick comments in local D1 now have non-empty route_tick_comments in the enriched JSON"
    - "21 routes that had route_tick_comments populated by the scraper still have non-empty values (not overwritten with empty string)"
    - "Routes with no D1 tick data retain their original route_tick_comments value (empty string or scraper value)"
    - "Unit tests for build_tick_map() and enrich_routes_with_tick_comments() pass"
  artifacts:
    - path: "tagging/enrich_ticks.py"
      provides: "D1 tick export + Yosemite route JSON enrichment script"
      exports: ["build_tick_map", "enrich_routes_with_tick_comments", "main"]
    - path: "tagging/tests/test_enrich_ticks.py"
      provides: "Unit tests for enrichment logic"
      contains: "test_build_tick_map, test_enrich_routes_with_tick_comments, test_preserve_scraper_ticks"
    - path: "data/yosemite-national-park_routes_enriched.json"
      provides: "Enriched Yosemite route JSON with D1 tick comments merged in"
  key_links:
    - from: "tagging/enrich_ticks.py"
      to: "worker-api/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite"
      via: "sqlite3.connect() direct read"
      pattern: "sqlite3\\.connect"
    - from: "tagging/enrich_ticks.py"
      to: "data/yosemite-national-park_routes.json"
      via: "json.load()"
      pattern: "yosemite-national-park_routes\\.json"
---

<objective>
Create enrich_ticks.py: a standalone script that reads tick comments from local D1 (direct sqlite3, 4,209 comments, 174 routes), merges them into data/yosemite-national-park_routes.json as the route_tick_comments field, and writes data/yosemite-national-park_routes_enriched.json. Write unit tests that verify merge logic, ID matching, and preservation of existing scraper-populated comments.

Purpose: The tagging pipeline prompt at line 185 of route_area_tagging.py already has a route_tick_comments slot. Currently only 21 Yosemite routes have this populated from the scraper; 174 routes have richer tick data in D1. The enriched JSON is the input for both the benchmark (Plan 02) and the full tagging run (Plan 04). Per D-05 and D-06.

Output: tagging/enrich_ticks.py, tagging/tests/test_enrich_ticks.py, data/yosemite-national-park_routes_enriched.json
</objective>

<execution_context>
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhirono/Dev/mtnpj/.planning/ROADMAP.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-CONTEXT.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-RESEARCH.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/02-tick-comments/02-04-SUMMARY.md
@/Users/jhirono/Dev/mtnpj/tagging/route_area_tagging.py
</context>

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

Local D1 path (confirmed by direct sqlite3 query):
  /Users/jhirono/Dev/mtnpj/worker-api/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/f3d7c63fe182b61452963d9a0ce4f8686130cdcc9c597caa56d15533e744f699.sqlite
  (glob pattern: worker-api/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite, skip metadata.sqlite)

comments table schema (PRAGMA table_info):
  comment_id TEXT (PK), parent_id TEXT, parent_type TEXT, comment_author TEXT, comment_text TEXT, comment_time TEXT

Current counts (verified):
  SELECT COUNT(*) FROM comments WHERE parent_type='tick'  → 4209
  SELECT COUNT(DISTINCT parent_id) FROM comments WHERE parent_type='tick'  → 174

D1 tick query (returns route_id, concatenated comments):
  SELECT parent_id, GROUP_CONCAT(comment_text, ' | ')
  FROM comments WHERE parent_type='tick' GROUP BY parent_id

route_area_tagging.py line 185 (the slot being populated):
  Comments: {route.get('route_tick_comments', '')} {' '.join([c.get('comment_text', '') for c in route.get('route_comments', [])])}

Existing test pattern (from tagging/tests/test_d1_tag_sync.py):
  from tagging.d1_tag_sync import (build_route_tag_updates, ...)
  # Tests use in-memory sqlite3 / fixture JSON — no real D1 access
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for enrichment logic</name>
  <files>tagging/tests/test_enrich_ticks.py</files>
  <read_first>
    - tagging/tests/test_d1_tag_sync.py (test structure and fixture pattern to replicate)
    - tagging/tests/__init__.py (package structure)
  </read_first>
  <behavior>
    - test_build_tick_map_returns_dict_keyed_by_route_id: given a mock sqlite3 connection with 2 tick rows for the same route_id, build_tick_map() returns {route_id: "comment1 | comment2"}
    - test_build_tick_map_excludes_non_tick_types: rows with parent_type='comment' are not included in the returned dict
    - test_enrich_routes_overwrites_empty_route_tick_comments: route with route_tick_comments='' and route_id in tick_map → route_tick_comments becomes the D1 value
    - test_enrich_routes_preserves_scraper_tick_comments: route with existing route_tick_comments='some scraper text' whose route_id is NOT in tick_map → value is preserved unchanged
    - test_enrich_routes_overwrites_when_route_id_in_d1: route with route_tick_comments='old scraper text' whose route_id IS in tick_map → value is replaced with D1 value (D1 is richer)
    - test_enrich_routes_route_id_cast_to_str: route_id=105733804 (int) in JSON matches tick_map key '105733804' (str from sqlite3 GROUP_CONCAT)
  </behavior>
  <action>
    Create tagging/tests/test_enrich_ticks.py. The module under test is tagging.enrich_ticks
    (the file created in Task 2), but tests are written RED-first so they will initially fail.

    Import structure:
      from tagging.enrich_ticks import build_tick_map, enrich_routes_with_tick_comments

    Use in-memory sqlite3 for build_tick_map tests — do NOT connect to the real D1 file.
    Mock pattern: create a sqlite3.connect(":memory:") fixture that has the comments table,
    insert test rows, and pass the db path (or monkeypatch glob.glob) so build_tick_map()
    uses the in-memory DB.

    Alternatively (simpler): extract the pure-function query logic so tests can call a helper
    that accepts a sqlite3.Connection directly, rather than a file path. This avoids glob
    mocking entirely. Preferred approach: make build_tick_map() accept an optional `db_path`
    parameter; tests pass ":memory:" and pre-populate the table.

    Sample test data:
      route_id='105733804', parent_type='tick', comment_text='Great climb'
      route_id='105733804', parent_type='tick', comment_text='Bring extra gear'
      route_id='999888777', parent_type='comment', comment_text='Should be excluded'

    Expected: build_tick_map(db_path) returns {'105733804': 'Great climb | Bring extra gear'}
    (or similar GROUP_CONCAT result — GROUP_CONCAT order is SQLite-implementation-dependent,
    so test that '105733804' is in the result and the value contains both strings).
  </action>
  <verify>
    <automated>cd /Users/jhirono/Dev/mtnpj && python3 -m pytest tagging/tests/test_enrich_ticks.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    All 6 test functions exist in the file. Tests that import from tagging.enrich_ticks
    will show ImportError or FAILED (not ERROR from syntax) because the module doesn't exist yet.
    At least the test file itself is syntactically valid: python3 -c "import ast; ast.parse(open('tagging/tests/test_enrich_ticks.py').read())" exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement enrich_ticks.py and run enrichment</name>
  <files>tagging/enrich_ticks.py</files>
  <read_first>
    - tagging/route_area_tagging.py (lines 1-30 for imports pattern, line 185 for route_tick_comments slot)
    - tagging/tests/test_enrich_ticks.py (the failing tests from Task 1 — implement to make them pass)
    - data/yosemite-national-park_routes.json (first 200 bytes to confirm structure: list of area dicts with "routes" key)
  </read_first>
  <action>
    Create tagging/enrich_ticks.py with the following:

    MODULE CONSTANTS:
      D1_GLOB = "worker-api/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite"
      INPUT_JSON = "data/yosemite-national-park_routes.json"
      OUTPUT_JSON = "data/yosemite-national-park_routes_enriched.json"

    FUNCTION: build_tick_map(db_path: str = None) -> dict[str, str]
      If db_path is None, use glob.glob(D1_GLOB) and pick the non-metadata file.
      Execute: SELECT parent_id, GROUP_CONCAT(comment_text, ' | ')
               FROM comments WHERE parent_type='tick' GROUP BY parent_id
      Return {str(row[0]): row[1] for row in rows}
      Print how many routes have tick data: "Loaded tick comments for {N} routes from D1"

    FUNCTION: enrich_routes_with_tick_comments(data: list, tick_map: dict) -> list
      Walk: for area in data → for route in area.get("routes", [])
        route_id = str(route.get("route_id", ""))
        if route_id in tick_map:
            route["route_tick_comments"] = tick_map[route_id]
        # else: DO NOT modify route_tick_comments — preserve existing value (scraper data or empty string)
      Return data

    FUNCTION: main()
      1. Call build_tick_map() with no args (uses real D1 glob)
      2. Load INPUT_JSON
      3. Call enrich_routes_with_tick_comments(data, tick_map)
      4. Write OUTPUT_JSON (json.dump, no indent — match existing data file style)
      5. Count enriched routes: sum(1 for area in data for route in area.get("routes",[])
                                    if route.get("route_tick_comments"))
      6. Print: "Enriched {N} of {total} routes with D1 tick comments → {OUTPUT_JSON}"

    if __name__ == "__main__": main()

    After writing the file, run the enrichment:
      cd /Users/jhirono/Dev/mtnpj && python3 -m tagging.enrich_ticks

    Expected output contains "Enriched" and a number between 150 and 200 (the 174 D1 routes
    plus the 21 pre-existing scraper routes, with possible overlap counted once).
  </action>
  <verify>
    <automated>cd /Users/jhirono/Dev/mtnpj && python3 -m pytest tagging/tests/test_enrich_ticks.py -q && python3 -c "import json; data=json.load(open('data/yosemite-national-park_routes_enriched.json')); enriched=sum(1 for a in data for r in a.get('routes',[]) if r.get('route_tick_comments')); total=sum(len(a.get('routes',[])) for a in data); print(f'Enriched {enriched}/{total} routes'); assert total==2966, f'Expected 2966 routes, got {total}'; assert enriched >= 150, f'Expected >=150 enriched routes, got {enriched}'"</automated>
  </verify>
  <acceptance_criteria>
    All 6 tests in test_enrich_ticks.py pass (GREEN).
    data/yosemite-national-park_routes_enriched.json exists with exactly 2,966 routes.
    At least 150 routes have non-empty route_tick_comments (174 from D1 + 21 scraper, minus any overlap).
    Routes not in tick_map retain original route_tick_comments (verified by test_preserve_scraper_tick_comments).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local D1 → Python process | sqlite3 reads comment_text from tick comments; untrusted user-generated text enters the route JSON |
| enriched JSON → tagging prompt | route_tick_comments is injected into the LLM prompt template at line 185; malformed text is benign (LLM ignores control chars) but very long values could inflate token cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | enrich_ticks.py: route["route_tick_comments"] assignment | mitigate | Do not eval/exec tick text; treat as opaque string. No SQL injection risk (read-only operation here). When this text flows to d1_tag_sync.py later, sql_quote() in chunk_updates() handles apostrophes. |
| T-03-02 | Denial of Service | build_tick_map: GROUP_CONCAT on 4,209 rows | accept | Low risk — local-only operation on a 4,209-row table; no user-facing latency impact. GROUP_CONCAT result size bounded by SQLite default (1,000,000 bytes); tick comments are short user notes. |
| T-03-03 | Information Disclosure | data/yosemite-national-park_routes_enriched.json written to disk | accept | File is gitignored (same as input JSON). Local developer machine only. No network exposure. |
</threat_model>

<verification>
1. `python3 -m pytest tagging/tests/test_enrich_ticks.py -q` — all 6 tests pass
2. `python3 -c "import json; data=json.load(open('data/yosemite-national-park_routes_enriched.json')); print(sum(len(a.get('routes',[])) for a in data))"` → 2966
3. `python3 -c "import json; data=json.load(open('data/yosemite-national-park_routes_enriched.json')); n=sum(1 for a in data for r in a.get('routes',[]) if r.get('route_tick_comments')); print(n)"` → value >= 150
4. `python3 -m pytest tagging/tests/ -q` — all existing tests still pass (39 base + 6 new)
</verification>

<success_criteria>
- data/yosemite-national-park_routes_enriched.json exists with 2,966 routes
- At least 150 routes have non-empty route_tick_comments (174 from D1, plus scraper routes)
- 6 new unit tests pass in tagging/tests/test_enrich_ticks.py
- All 39 pre-existing tests still pass
- Routes without D1 tick data are not modified (scraper values preserved)
</success_criteria>

<output>
After completion, create /Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-01-SUMMARY.md
</output>
