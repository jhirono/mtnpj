---
phase: 03-tagging-upgrade
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tagging/benchmark_models.py
autonomous: false
requirements: [TAG-01, TAG-04]
must_haves:
  truths:
    - "benchmark_models.py submits 3 batch jobs (gpt-4o-mini, gpt-5-nano, gpt-5-mini) against a 50-route sample"
    - "benchmark_results.json exists with per-route tags from each model for side-by-side comparison"
    - "gpt-5-mini PermissionDeniedError is caught gracefully — script does not crash, reports error and continues with remaining models"
    - "User reviews the 50-route output and selects the winning model before Wave 2 proceeds"
  artifacts:
    - path: "tagging/benchmark_models.py"
      provides: "3-model benchmark orchestrator using OpenAI Batch API"
      exports: ["create_batch_requests_for_model", "run_benchmark"]
    - path: "benchmark_results.json"
      provides: "Side-by-side tag comparison for user review"
  key_links:
    - from: "tagging/benchmark_models.py"
      to: "OpenAI Batch API"
      via: "client.batches.create()"
      pattern: "client\\.batches\\.create"
    - from: "tagging/benchmark_models.py"
      to: "data/yosemite-national-park_routes_enriched.json (or fallback to non-enriched)"
      via: "json.load()"
      pattern: "yosemite-national-park_routes"
---

<objective>
Create benchmark_models.py: a standalone script that submits 50 Yosemite routes to three OpenAI Batch API models (gpt-4o-mini, gpt-5-nano, gpt-5-mini) concurrently, polls until all batches complete, and writes benchmark_results.json with per-route tag outputs from each model for user review. Includes graceful handling of the gpt-5-mini PermissionDeniedError batch provisioning bug.

Purpose: TAG-01 requires benchmarking the three candidate models to select the winner. Per D-01 (same Batch API client, model string swap only), D-02 (50 routes), D-03 (accuracy-first then cost), D-10/D-11 (user reviews benchmark before full run). The user checkpoint after this plan is MANDATORY — no Wave 2 plans can run until the user selects the winning model.

Output: tagging/benchmark_models.py, benchmark_results.json (written after batch completion)
</objective>

<execution_context>
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhirono/Dev/mtnpj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jhirono/Dev/mtnpj/.planning/ROADMAP.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-CONTEXT.md
@/Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-RESEARCH.md
@/Users/jhirono/Dev/mtnpj/tagging/route_area_tagging.py
</context>

<interfaces>
<!-- Key interfaces from route_area_tagging.py the executor must replicate -->

route_area_tagging.py:create_batch_requests() (lines 175-205):
```python
def create_batch_requests(routes, prompt_template: str) -> List[Dict]:
    # For each route, builds request body with:
    #   "model": "gpt-4o-mini"           ← benchmark script parameterizes this
    #   "custom_id": str(route.get('route_id'))
    #   "method": "POST", "url": "/v1/chat/completions"
    #   "body": {"messages": [...], "temperature": 0.3, "max_tokens": 500, "top_p": 0.95, "n": 1}
    # Input text format (line 180-186):
    #   Route Description: {route.get('route_description', '')}
    #   Route Location: {route.get('route_location', '')}
    #   Route Type: {route.get('route_type', '')}
    #   Route Protection: {route.get('route_protection', '')}
    #   Comments: {route.get('route_tick_comments', '')} {' '.join([c.get('comment_text','') for c in route.get('route_comments',[])])}
```

route_area_tagging.py:submit_batch() (lines 207-246):
```python
def submit_batch(batch_requests, retry_count=0) -> str:
    # Writes JSONL to file, uploads via client.files.create(purpose="batch"),
    # calls client.batches.create(input_file_id=..., endpoint="/v1/chat/completions", completion_window="24h")
    # Returns batch.id string
```

route_area_tagging.py:wait_for_batch_completion() (lines 248-335):
```python
def wait_for_batch_completion(batch_id, check_only=False):
    # Polls every 10 seconds until batch.status == "completed"
    # Handles comma-separated batch IDs
    # Returns list of result dicts on completion
```

Prompt template files:
  tagging/prompts/route_tagging_prompt.txt  (check if exists — used by route_area_tagging.py)

OpenAI error to catch:
  openai.PermissionDeniedError  (HTTP 403 on client.batches.create())
  Fallback snapshot ID if needed: "gpt-5-mini-2025-08-07"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement benchmark_models.py</name>
  <files>tagging/benchmark_models.py</files>
  <read_first>
    - tagging/route_area_tagging.py (lines 1-30 for client/imports, lines 175-246 for batch request creation + submit_batch, lines 248-335 for wait_for_batch_completion — copy/adapt these functions)
    - tagging/route_area_tagging.py (grep for prompt file path — find where route_tagging_prompt is loaded, typically in process_areas_and_routes around line 516)
  </read_first>
  <action>
    Create tagging/benchmark_models.py. This script is run AFTER Plan 01 completes
    (data/yosemite-national-park_routes_enriched.json exists) but can also fall back
    to data/yosemite-national-park_routes.json if the enriched file is missing.

    CONSTANTS:
      BENCHMARK_MODELS = ["gpt-4o-mini", "gpt-5-nano", "gpt-5-mini"]
      SAMPLE_SIZE = 50
      OUTPUT_FILE = "benchmark_results.json"
      ENRICHED_INPUT = "data/yosemite-national-park_routes_enriched.json"
      FALLBACK_INPUT = "data/yosemite-national-park_routes.json"

    CLIENT SETUP (same as route_area_tagging.py lines 14-16):
      import openai, os
      openai.api_key = os.getenv("OPENAI_API_KEY")
      client = openai.OpenAI()

    FUNCTION: create_batch_requests_for_model(routes: list, prompt_template: str, model: str) -> list
      Identical to route_area_tagging.py:create_batch_requests() EXCEPT:
      - Takes `model: str` parameter instead of hardcoding "gpt-4o-mini"
      - Uses model in the request body: "model": model
      - custom_id prefix: f"{model}__{route_id}" so results from different models
        can be distinguished (custom_ids must be unique within a batch, but across
        batches they can overlap — the prefix ensures unambiguous result parsing)

    FUNCTION: select_sample(data: list, size: int = 50) -> list
      Flatten all routes from all areas into a single list.
      Split into: routes_with_ticks = [r for r in all_routes if r.get('route_tick_comments')]
                  routes_without = [r for r in all_routes if not r.get('route_tick_comments')]
      Sample: take min(25, len(routes_with_ticks)) from routes_with_ticks,
              fill remainder from routes_without to reach size=50.
      This ensures the 50-route sample has a mix per D-02.
      Use random.sample() with random.seed(42) for reproducibility.

    FUNCTION: submit_batch_for_model(batch_requests: list, model: str) -> str | None
      Try submit_batch(batch_requests) — reuse directly from route_area_tagging.py.
      BUT wrap client.batches.create() call: catch openai.PermissionDeniedError.
      If caught:
        print(f"[WARNING] gpt-5-mini batch access denied (403). Try snapshot ID 'gpt-5-mini-2025-08-07' or create a new API project.")
        print("Benchmark will continue with remaining models. gpt-5-mini result will be missing.")
        return None
      Return batch_id string on success.

    IMPORTANT: The simplest approach is to copy submit_batch() from route_area_tagging.py
    into benchmark_models.py and add the PermissionDeniedError try/except around the
    client.batches.create() call specifically (not the whole function). Do NOT import
    from route_area_tagging directly — that module has global side effects (openai.api_key
    assignment at import time is fine, but it also has LOG_FILE setup, etc.).

    FUNCTION: run_benchmark(data: list, prompt_template: str) -> dict
      1. sample = select_sample(data, SAMPLE_SIZE)
      2. For each model in BENCHMARK_MODELS:
         - requests = create_batch_requests_for_model(sample, prompt_template, model)
         - batch_id = submit_batch_for_model(requests, model)
         - if batch_id: batch_ids[model] = batch_id
         - else: batch_ids[model] = None (skipped due to 403)
      3. Print submitted batch IDs for all models.
      4. Poll: for model, batch_id in batch_ids.items() where batch_id is not None:
           results[model] = wait_for_batch_completion(batch_id)
      5. Return {model: results_list} for successful models; None for 403-skipped models.

    FUNCTION: write_comparison(sample: list, results: dict, output_path: str)
      Build a comparison list:
        for each route in sample:
          route_id = str(route.get('route_id'))
          entry = {
            "route_id": route_id,
            "route_name": route.get("route_name", ""),
            "has_ticks": bool(route.get("route_tick_comments")),
            "models": {}
          }
          for model, model_results in results.items():
            if model_results is None:
              entry["models"][model] = {"error": "PermissionDeniedError (403)"}
            else:
              # Find the result with custom_id == f"{model}__{route_id}"
              match = next((r for r in model_results
                            if r.get("custom_id") == f"{model}__{route_id}"), None)
              entry["models"][model] = parse_tags_from_result(match) if match else {"error": "no result"}
        json.dump(comparison_list, open(output_path, "w"), indent=2)
      Print: f"Benchmark results written to {output_path} — review and select winning model"

    FUNCTION: parse_tags_from_result(result: dict) -> dict
      Extract the JSON content from the batch result's choices[0].message.content.
      This mirrors process_batch_results() in route_area_tagging.py.
      Return the parsed tags dict, or {"error": "parse_failed"} on JSONDecodeError.

    FUNCTION: main()
      1. Verify OPENAI_API_KEY is set: os.getenv("OPENAI_API_KEY") — sys.exit(1) with message if missing.
      2. Load enriched JSON if exists, else fallback:
         input_file = ENRICHED_INPUT if os.path.exists(ENRICHED_INPUT) else FALLBACK_INPUT
         print(f"Using input: {input_file}")
      3. Load prompt template. Check tagging/prompts/route_tagging_prompt.txt (or wherever
         route_area_tagging.py loads it — read the file to find the actual path).
      4. data = json.load(open(input_file))
      5. sample = select_sample(data, SAMPLE_SIZE)
      6. Print sample stats: how many have ticks, how many don't.
      7. results = run_benchmark(data, prompt_template)
      8. write_comparison(sample, results, OUTPUT_FILE)

    if __name__ == "__main__": main()

    AFTER WRITING the file, verify syntax only (do NOT run yet — running submits real API calls):
      python3 -c "import ast; ast.parse(open('tagging/benchmark_models.py').read()); print('Syntax OK')"
  </action>
  <verify>
    <automated>cd /Users/jhirono/Dev/mtnpj && python3 -c "import ast; ast.parse(open('tagging/benchmark_models.py').read()); print('Syntax OK')" && grep -n "PermissionDeniedError" tagging/benchmark_models.py | grep -c "except\|catch" | grep -v "^0$" && grep -c '"gpt-4o-mini"' tagging/benchmark_models.py && grep -c '"gpt-5-nano"' tagging/benchmark_models.py && grep -c '"gpt-5-mini"' tagging/benchmark_models.py</automated>
  </verify>
  <acceptance_criteria>
    - tagging/benchmark_models.py exists and is syntactically valid (ast.parse exits 0)
    - File contains `BENCHMARK_MODELS = ["gpt-4o-mini", "gpt-5-nano", "gpt-5-mini"]`
    - `grep -n "PermissionDeniedError" tagging/benchmark_models.py` returns at least 1 line with an except clause
    - `grep -n 'gpt-5-mini-2025-08-07' tagging/benchmark_models.py` returns at least 1 line (fallback snapshot ID mentioned in error guidance)
    - `grep -n 'model' tagging/benchmark_models.py | grep "create_batch_requests_for_model"` confirms the parameterized function exists
    - `grep -c 'custom_id.*model' tagging/benchmark_models.py` returns >= 1 (model-prefixed custom_id)
    - `grep -n 'random.seed(42)' tagging/benchmark_models.py` returns 1 line (reproducible sample)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run benchmark + user model selection</name>
  <read_first>
    - benchmark_results.json (after benchmark completes — will not exist until you run the script)
    - tagging/benchmark_models.py (to understand what the output means)
  </read_first>
  <what-built>
    benchmark_models.py — submits 50-route sample to gpt-4o-mini, gpt-5-nano, and gpt-5-mini
    in parallel batch jobs, then writes benchmark_results.json with per-route tag comparison.
  </what-built>
  <how-to-verify>
    Step 1 — Set OPENAI_API_KEY if not already set:
      export OPENAI_API_KEY=$(grep OPENAI_API_KEY .env | cut -d= -f2)

    Step 2 — Run the benchmark (this submits real API calls, cost < $0.05 total):
      cd /Users/jhirono/Dev/mtnpj && python3 -m tagging.benchmark_models

    Step 3 — Wait for batch completion. The script polls every 10 seconds. Batches for
    50 routes typically complete in minutes (not 24h). Estimated wall time: 5-15 minutes.
    If gpt-5-mini returns a 403, the script will print a WARNING and continue with the
    other two models — this is expected and handled.

    Step 4 — Review benchmark_results.json. For each route, compare the tags from each
    model. Focus on:
      - Tag accuracy: do the tags make sense for the route (sun exposure, crowds, access)?
      - Tick comment impact: do routes with has_ticks=true get noticeably better tags?
      - Any hallucinated or invalid tags?

    Step 5 — Select the winning model using this framework (D-03: accuracy-first, then cost):
      - gpt-5-nano: cheapest ($0.19 for full 2,969 run), newest reasoning. Winner if accuracy >= gpt-4o-mini.
      - gpt-4o-mini: current baseline ($0.35 for full run). Winner if gpt-5-nano noticeably worse.
      - gpt-5-mini: most expensive ($0.93 for full run). Winner only if clearly more accurate than nano.
      - If gpt-5-mini had a 403 error: select from gpt-4o-mini vs gpt-5-nano only.

    Step 6 — Type your selection below. Format: the exact model ID string.
      Options: "gpt-4o-mini" | "gpt-5-nano" | "gpt-5-mini"
  </how-to-verify>
  <resume-signal>
    Type the winning model ID exactly as it should appear in the Python code:
    "gpt-4o-mini" or "gpt-5-nano" or "gpt-5-mini"
    Then type "approved" to confirm Wave 2 can proceed.
    Example: "gpt-5-nano approved"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → OpenAI Batch API | OPENAI_API_KEY leaves the machine; batch requests contain route descriptions (public MP data) |
| OpenAI Batch API → local JSON | LLM output (tag JSON strings) is parsed and written to benchmark_results.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | OPENAI_API_KEY in environment | mitigate | Key loaded via os.getenv() (not hardcoded). .env file is gitignored. Never log the key value. |
| T-03-05 | Tampering | parse_tags_from_result(): LLM JSON output parsed | mitigate | Wrap json.loads() in try/except JSONDecodeError; return {"error": "parse_failed"} on failure. Same pattern as process_batch_results() in route_area_tagging.py. |
| T-03-06 | Denial of Service | OpenAI Batch API cost | accept | 50-route benchmark costs <$0.05 total across all three models. Cost is explicitly displayed before submission. |
| T-03-07 | Repudiation | No audit log of which model was selected | accept | benchmark_results.json + user checkpoint in SUMMARY.md provides sufficient audit trail for a solo dev workflow. |
</threat_model>

<verification>
1. `python3 -c "import ast; ast.parse(open('tagging/benchmark_models.py').read()); print('OK')"` — Syntax OK
2. `grep -n "PermissionDeniedError" tagging/benchmark_models.py` — at least 1 line
3. `grep -n "gpt-5-mini-2025-08-07" tagging/benchmark_models.py` — at least 1 line
4. After run: `ls -lh benchmark_results.json` — file exists with size > 0
5. After run: `python3 -c "import json; r=json.load(open('benchmark_results.json')); print(len(r), 'routes compared')"` → 50
</verification>

<success_criteria>
- benchmark_models.py written and syntactically valid
- gpt-5-mini PermissionDeniedError is caught — script does not crash on 403
- Benchmark executes against real OpenAI Batch API with 50-route sample
- benchmark_results.json contains per-route tag comparison across all available models
- User has reviewed benchmark_results.json and selected a winning model
- Wave 2 (Plans 03 and 04) are unblocked with the winning model ID confirmed
</success_criteria>

<output>
After completion, create /Users/jhirono/Dev/mtnpj/.planning/phases/03-tagging-upgrade/03-02-SUMMARY.md
Record the winning model ID selected by the user — this is required input for Plan 03.
</output>
